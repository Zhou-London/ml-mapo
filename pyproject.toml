[project]
name = "ml-mapo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.1",
    "torch>=2.10.0",
    "yfinance>=1.2.0",
]
